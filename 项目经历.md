# Unity技能编辑器项目经历

## 项目概述
这是一个基于Unity引擎开发的通用技能编辑器框架项目（MochiFramework.SkillEditor），旨在为游戏开发提供可视化的技能配置和编辑工具。该编辑器采用时间轴+轨道的设计模式，支持多种类型的轨道和片段，允许开发者通过直观的界面设计和配置游戏角色的技能效果。

## 核心功能模块

### 1. 时间轴编辑系统
- **可视化时间轴**：提供基于帧的时间轴界面，支持精确到帧的编辑
- **缩放功能**：支持通过鼠标滚轮调整时间轴的显示比例
- **选择线**：提供当前帧选择功能，支持拖拽选择
- **播放控制**：包含播放、暂停、逐帧前进/后退等控制功能

### 2. 轨道系统
采用泛型设计的轨道架构，支持多种轨道类型：

#### 内置轨道类型
- **动画轨道（AnimationTrack）**：支持Unity AnimationClip的播放和混合
- **音频轨道（AudioTrack）**：支持Unity AudioClip的播放
- **预制体轨道（PrefabTrack）**：支持在指定时间点生成和销毁预制体对象

#### 扩展轨道
- **FMOD轨道（FmodTrack）**：集成FMOD音频系统，支持更复杂的音频事件管理

### 3. 片段管理系统
- **拖拽插入**：支持从Project窗口拖拽资源到轨道上自动创建片段
- **片段移动**：支持在轨道上拖拽移动片段位置
- **片段删除**：支持右键菜单删除片段
- **长度调整**：支持重置片段到原始长度
- **碰撞检测**：自动检测片段间的碰撞，防止重叠

### 4. 槽位映射系统
- **槽位定义（SlotDefinition）**：定义角色上的挂载点
- **槽位映射（SlotMapper）**：将预制体绑定到角色的特定槽位
- **可视化配置**：在编辑器中可视化配置槽位关系

### 5. 预览系统
- **预览场景**：独立的预览场景，不影响游戏场景
- **实时预览**：在编辑器中实时预览技能效果
- **角色切换**：支持切换不同的预览角色模型

## 技术架构

### 1. 数据层（Runtime）
- **SkillConfig**：技能配置数据，使用ScriptableObject存储
- **Track<TClip>**：泛型轨道基类，管理片段集合
- **Clip**：片段基类，定义片段的基本属性
- **ISkillPlayer**：技能播放器接口

### 2. 运行时层（Runtime）
- **SkillPlayer**：技能播放器，负责技能的运行时播放
- **TrackHandler**：轨道处理器，处理轨道在运行时的行为
- **AnimationTrackHandler**：动画轨道处理器
- **AudioTrackHandler**：音频轨道处理器
- **PrefabTrackHandler**：预制体轨道处理器

### 3. 编辑器层（Editor）
- **SkillEditor**：主编辑器窗口，基于UIElements构建
- **TrackView**：轨道视图，负责轨道的显示和交互
- **ClipView**：片段视图，负责片段的显示和交互
- **SkillPreviewPlayer**：预览播放器，管理预览场景

### 4. 扩展系统
- **CustomTrackAttribute**：自定义轨道属性标记
- **CustomClipAttribute**：自定义片段属性标记
- **反射机制**：通过反射自动发现和注册自定义轨道类型

## 技术亮点

### 1. 泛型设计模式
采用泛型设计实现灵活的轨道和片段系统：
```csharp
public abstract class Track<TClip> : ITrack where TClip : Clip
```
这种设计允许开发者轻松扩展新的轨道类型，同时保持类型安全。

### 2. 属性驱动扩展
使用自定义属性实现扩展点的声明式编程：
```csharp
[CustomTrack(DefaultName = "动画轨道", IsUnique = true)]
public sealed class AnimationTrack : Track<AnimationClip>
```
编辑器通过反射自动发现和注册标记的轨道类型。

### 3. 多态序列化
使用Unity的[SerializeReference]特性实现多态序列化：
```csharp
[SerializeReference] public List<ITrack> tracks;
```
这允许在同一个列表中存储不同类型的轨道对象。

### 4. UIElements架构
采用Unity最新的UIElements框架构建编辑器界面：
- 基于UXML和USS的声明式UI定义
- 响应式布局系统
- 高性能的渲染机制

### 5. 撤销/重做集成
深度集成Unity的Undo系统：
```csharp
Undo.RegisterCompleteObjectUndo(track.SkillConfig, "Move Clip");
```
支持对编辑操作的撤销和重做。

### 6. 拖拽系统
实现灵活的拖拽交互：
- 支持从Project窗口拖拽资源
- 支持轨道间的片段拖拽
- 自动类型转换和验证

## 已实现功能清单

### 新增功能
- ✅ 实现预制体轨道功能
- ⏳ 在编辑器中调整Clip长度
- ⏳ 集成FMOD轨道

### 修复缺陷
- ✅ 修复Clip在拖拽插入时，位置归0的问题
- ✅ 修复SlotMapper在编辑器中修改后，无法序列化的问题
- ⏳ 移动Clip时鼠标超出ClipView的范围就会失效

## 项目特色

### 1. 高度可扩展
- 支持自定义轨道类型
- 支持自定义片段类型
- 支持自定义轨道处理器

### 2. 用户友好
- 直观的拖拽操作
- 实时预览功能
- 完整的撤销/重做支持

### 3. 性能优化
- 基于帧的精确控制
- 高效的碰撞检测算法
- 按需更新的视图系统

### 4. 工程化实践
- 清晰的代码分层
- 完善的接口定义
- 良好的注释文档

## 应用场景
该技能编辑器适用于以下游戏开发场景：
- 动作游戏的技能系统配置
- RPG游戏的战斗效果设计
- MOBA游戏的英雄技能配置
- 卡牌游戏的技能效果管理
- 任何需要时间轴控制的特效系统

## 技术栈
- **引擎**：Unity 2021+
- **语言**：C#
- **UI框架**：UIElements
- **音频系统**：Unity Audio + FMOD
- **动画系统**：Unity Animator + Animancer
- **序列化**：Unity ScriptableObject + SerializeReference

## 项目文件结构
```
Assets/
├── MochiFramework/
│   ├── SkillEditor/
│   │   ├── Editor/           # 编辑器相关代码
│   │   └── Runtime/           # 运行时相关代码
│   └── Helpers/               # 辅助工具类
├── Example/                   # 示例场景和资源
│   ├── Jammo-Character/       # 示例角色
│   └── FmodIntegration/       # FMOD集成示例
└── Plugins/
    └── FMOD/                  # FMOD插件
```

## 总结
该项目是一个设计精良、架构清晰的Unity技能编辑器框架。通过采用泛型设计、属性驱动、多态序列化等先进技术，实现了高度可扩展的编辑器系统。项目不仅提供了完整的可视化编辑功能，还通过良好的架构设计为后续扩展奠定了坚实基础。该编辑器可以显著提升游戏开发中技能系统的配置效率，是一个具有实际应用价值的工具项目。